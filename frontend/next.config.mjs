/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'upload.wikimedia.org',
      'cdn.worldvectorlogo.com',
      'cdn.jsdelivr.net',
      'localhost',
    ],
  },
}

export default nextConfig
